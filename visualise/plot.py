# visualize/plot.py

import matplotlib.pyplot as plt

#def plot_equity_curve(df, buy_and_hold, metrics):
    # df['total_value'], buy_and_hold series, annotate metrics

#def plot_signals(price_df, signals):
    # price + markers for buy/sell

#def plot_drawdown(df):
    # compute running max and drawdown plot
